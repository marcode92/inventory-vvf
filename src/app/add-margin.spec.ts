import { AddMargin } from './add-margin';

describe('AddMargin', () => {
  it('should create an instance', () => {
    const directive = new AddMargin();
    expect(directive).toBeTruthy();
  });
});

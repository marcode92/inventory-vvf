export interface InventoryItem {
  cat_tipo?: string,
  num_inv?: string,
  sec_pdci?: string,
  denominazione?: string,
  matricola?: string,
  annotazioni?: string,
  stanza?: string,
  possessori?: string,
  last_update?: string,
  fuori_uso?: boolean,
}